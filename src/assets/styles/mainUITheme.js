import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
      primary: {
        main: "#FFD275", // yellow
      },
      secondary: {
        main: "#FFFFFF", //white
      },
    },
  });

export default theme;