import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
      primary: {
        main: "#FFD275", // yellow
      },
      secondary: {
        main: "#DB5A42", //red
      },
    },
  });

export default theme;